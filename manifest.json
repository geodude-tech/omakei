{
  "schemaVersion": 1,
  "id": "omakei",
  "name": "Omakei",
  "version": "1.0.0",
  "author": "Andrew Scholz",
  "license": "MIT",
  "description": "This month's leftover on the Omarchy bar. Attach a folder of statements on this machine. Nothing is uploaded.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Omakei",
    "description": "This month's leftover after spend and set-asides",
    "category": "Info",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "appUrl": "http://127.0.0.1:8080/"
    },
    "schema": [
      {
        "key": "ledgerPath",
        "type": "string",
        "label": "Ledger file",
        "defaultValue": "",
        "description": "Optional. Omakei records the folder you attach in the editor and the widget reads the ledger from there. Set this only to point at a ledger somewhere else."
      },
      {
        "key": "appUrl",
        "type": "string",
        "label": "Omakei URL"
      }
    ]
  }
}
