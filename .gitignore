node_modules
.node_modules.lock
*.log
.env
.env.*
!.env.example
screenshots
artifacts
.DS_Store
*.local

# Isolated dev sandbox: `npm run dev:isolated` keeps its own state file and
# statements here, so developing never reads or rewrites the real ones.
.dev/

# Personal financial data — never commit real statements.
# folio-ledger.json is the name an early build used; still blocked so an old
# one cannot slip in.
omakei-ledger.json
folio-ledger.json
*.csv
*.tsv
*.ofx
*.qfx
*.ofc
Financial_Statements/
data/statements/
statements/

# Household-specific words the personal-data scanner blocks. Never committed:
# the block list would itself be the leak.
.githooks/personal-terms

# Claude Code's scratch worktrees: whole checkouts of this repo, never committed.
.claude/worktrees/

# Per-branch planning scratch from the spec-driven workflow (tasks/plan.md,
# tasks/todo.md). The durable artifact is the spec under docs/spec/; the plan
# and todo are working notes and only create merge noise on main.
tasks/
