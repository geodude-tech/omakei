node_modules
.node_modules.lock
*.log
.env
.env.*
!.env.example
screenshots
artifacts
.DS_Store
*.local

# Personal financial data — never commit real statements.
# folio-ledger.json is the name an early build used; still blocked so an old
# one cannot slip in.
omakei-ledger.json
folio-ledger.json
*.csv
*.tsv
*.ofx
*.qfx
*.ofc
Financial_Statements/
data/statements/
statements/
