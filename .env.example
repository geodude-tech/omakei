# Absolute path to statement exports on this machine.
# Used only by the local Vite dev server (localhost requests).
# Omakei auto-loads omakei-ledger.json from this folder when you open the app.
# Never put real CSVs in the git repo.
# FOLIO_STATEMENTS_DIR=/absolute/path/to/statements
